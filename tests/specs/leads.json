[
  {
    "name": "/leads denies access when owner access is not configured",
    "steps": [
      {
        "send": { "text": "/leads" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Owner access isn't set up yet." } }]
      }
    ]
  }
]
