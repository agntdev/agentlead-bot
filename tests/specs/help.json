[
  {
    "name": "/help explains the bot",
    "steps": [
      {
        "send": { "text": "/help" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Use Submit request to share your property plans.\n\nThe agent can open Owner from the main menu." } }]
      }
    ]
  },
  {
    "name": "Help menu button",
    "steps": [{ "send": { "callback": "menu:help" }, "expect": [{ "method": "editMessageText" }] }]
  }
]
