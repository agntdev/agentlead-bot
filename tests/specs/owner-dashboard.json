[
  {
    "name": "Owner dashboard denies access when it is not configured",
    "steps": [
      {
        "send": { "callback": "owner:dashboard" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Owner access isn't set up yet." } }]
      }
    ]
  }
]
